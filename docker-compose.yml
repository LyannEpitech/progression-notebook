services:
  dashboard:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./datasets:/app/datasets
      - ./plots_students:/app/plots_students
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
